{
  "name": "property-platform",
  "version": "1.0.0",
  "description": "Property platform monorepo",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
